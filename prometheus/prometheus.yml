global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'iso_guardian_backend'
    static_configs:
      - targets: ['host.docker.internal:8000']
        # If running Prometheus natively (not in Docker), use 'localhost:8000'
